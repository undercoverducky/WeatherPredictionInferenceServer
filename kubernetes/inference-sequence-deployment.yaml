apiVersion: machinelearning.seldon.io/v1
kind: SeldonDeployment
metadata:
  name: weather-sequence-prediction-deployment
spec:
  name: weather-sequence-prediction
  predictors:
  - componentSpecs:
    - spec:
        containers:
        - image: undercoverducky/weatherpredictionsequencemodel:0.0.2-amd64
          name: weather-sequence-regression
          ports:
            - containerPort: 9000
          env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: AWS_SECRET_ACCESS_KEY
        terminationGracePeriodSeconds: 1
    graph:
      children: []
      endpoint:
        type: REST
      name: weather-sequence-regression
      type: MODEL
    labels:
      version: v1
    name: weather-sequence-regression-component
    replicas: 1